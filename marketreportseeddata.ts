/**
 * Re-exports seed data from `convex/seedData/marketReportSeedData.ts` (the copy
 * Convex bundles for `npx convex run seedMarketReports:seedMarketReports`).
 */
export {
  marketReportsSeedData,
  type MarketReportSeedData,
} from "./convex/seedData/marketReportSeedData";
